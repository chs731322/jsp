<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.memberInfoMapper">

	<resultMap id="UsersDTO" type="dto.UsersDTO">
		<id property="memberNum" column="memberNum" />
		<result property="id" column="id" />
		<result property="name" column="name" />
		<result property="email" column="email" />
		<result property="birth" column="birth" />
		<result property="password" column="password" />
	</resultMap>

	<update id="updateMemberInfo" parameterType="dto.UsersDTO">
		UPDATE Users
		<set>
			<if test="id != null">
				id = #{id},
			</if>
			<if test="name != null">
				name = #{name},
			</if>
			<if test="email != null">
				email = #{email},
			</if>
			birth = #{birth},
			password = standard_hash(#{password}, 'SHA512')
			
		</set>
		WHERE memberNum = #{memberNum}
	</update>


	<!-- 사용자 삭제 쿼리 -->
	<delete id="deleteMember" parameterType="string">
		DELETE FROM Users WHERE memberNum = #{memberNum}
	</delete>

</mapper>